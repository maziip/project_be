# 3개월 자기주도 백엔드 전환 프로그램

문서 버전: 1.1  
목표 직무: NHN AI 서비스 개발 - Java/Spring 백엔드  
교육 기간: 12주  
운영 기준: 주 5일 집중 학습, 주차별 완료 조건에 따라 진도 조정


## 이 문서의 사용 방법

이 문서는 학습자가 혼자 읽고 진도를 관리하는 교재가 아니다. **AI Agent가 먼저 전체 문서를 읽고, 현재 상태를 기록하며, 매일의 학습·구현·리뷰·평가를 진행하기 위한 운영 기준**이다.

1. 첫 세션에서 AI Agent에게 이 문서 전체를 읽게 하고 `대상자_전달용_AI_Agent_시작_프롬프트.md` 또는 `8.3 시작 프롬프트`를 전달한다.
2. Agent는 학습 저장소에 `LEARNING_STATE.md`, `VERSIONS.md`를 만들고 매 세션 시작과 종료에 갱신한다.
3. 1주 차부터 순서대로 진행하고, 월별 관문을 통과하지 못하면 최대 3일간 보충한 뒤 다시 평가한다.
4. 매주 제시된 티켓을 Git 브랜치와 Pull Request 형식으로 처리한다.
5. Tutor Agent는 설명과 힌트를 담당하고, 평가는 학습 대화를 공유하지 않은 별도 Evaluator Agent 또는 새 대화에서 진행한다.
6. 매주 금요일에는 AI 없이 제한 시간 구현과 기능 설명을 먼저 진행한 뒤 AI 평가를 받는다.
7. 필수 완료 조건을 충족하기 전에는 STT/TTS, Kubernetes, 별도 프론트엔드 같은 심화 기능으로 넘어가지 않는다.
8. 코드, 테스트 결과, 회고, 설계 결정과 평가 증거를 모두 저장소에 기록한다.
9. 보충 후 재평가에도 실패하면 다음 달 핵심 기술로 진입하지 않는다. 심화·선택 항목을 제거하고 미달 영역을 다시 계획하며, 12주 안에 해소하지 못하면 최종 상태를 `추가 학습 필요`로 기록한다.

---

## 1. 프로그램의 목적

이 프로그램의 목표는 3개월 안에 백엔드의 모든 영역을 익히는 것이 아니다.

교육 종료 시점에 대상자가 다음 3개월 동안 AI 서비스 개발팀의 실제 업무를 받아 수행할 수 있는 **현업 투입 준비 상태**에 도달하는 것을 목표로 한다.

구체적으로는 다음 상태를 말한다.

- 기존 Spring Boot 프로젝트를 실행하고 구조를 파악할 수 있다.
- 작은 REST API와 DB 기능을 기존 코드 규칙에 맞게 추가할 수 있다.
- 단위 테스트와 통합 테스트를 작성할 수 있다.
- 외부 AI API를 연동하고 타임아웃과 실패를 처리할 수 있다.
- OpenSearch에 데이터를 색인하고 기본적인 검색 기능을 구현할 수 있다.
- 로그와 테스트를 이용해 문제 원인을 좁힐 수 있다.
- 요구사항 확인부터 설계, 구현, Pull Request, 리뷰 반영까지의 업무 흐름을 수행할 수 있다.
- 모르는 문제를 숨기지 않고 조사 내용과 가설을 정리해 도움을 요청할 수 있다.

대상자를 3개월 만에 숙련된 백엔드 개발자로 만드는 것은 목표가 아니다. 목표 포지셔닝은 다음과 같다.

> 백엔드 기술은 전환 단계이지만, 제품 개발·성능 개선·품질 관리·협업에서는 10년의 경험을 보유한 소프트웨어 엔지니어

---

## 2. 학습 기술 기준

- Java LTS 버전
- 공식 지원 중인 Spring Boot 안정 버전
- Gradle
- MySQL
- 공식 지원 중이며 Java client와 호환되는 OpenSearch 안정 버전
- Docker 및 Docker Compose

교육을 시작하는 날 Java-Spring Boot-Gradle-OpenSearch client의 호환성과 공식 지원 상태를 함께 확인한 뒤 `VERSIONS.md`에 버전을 고정한다. 특정 회사의 내부 구조를 추측하기보다 다른 Java/Spring 환경에도 적용할 수 있는 기본기를 우선한다.

---

## 3. 교육 운영 원칙

### 3.1 하나의 프로젝트를 12주 동안 발전시킨다

작은 예제를 반복해서 버리지 않는다. 첫 주에 저장소를 만들고 매주 같은 프로젝트에 기능과 품질을 누적한다.

### 3.2 학습 단위는 강의가 아니라 업무 티켓이다

각 개념은 실제 요구사항을 구현할 필요가 생겼을 때 학습한다. 모든 티켓은 다음 순서로 처리한다.

```text
티켓 확인
→ 모르는 용어와 요구사항 정리
→ 구현 계획 작성
→ 테스트 조건 정의
→ 구현
→ 자동화 테스트
→ Pull Request 형식의 설명 작성
→ Reviewer Agent 코드 리뷰
→ 리뷰 반영
→ 회고
```

### 3.3 동작보다 근거를 남긴다

모든 주요 구현에는 다음 증거를 남긴다.

- 왜 이 구조를 선택했는지
- 다른 선택지는 무엇이었는지
- 어떤 실패 조건을 고려했는지
- 무엇을 테스트했는지
- 알려진 한계는 무엇인지

### 3.4 새로운 기술보다 기본 동작과 실패 처리를 우선한다

STT/TTS, Kubernetes, 별도 프론트엔드는 선택 기능이다. 핵심 API, DB, 테스트, 오류 처리가 완료되지 않았다면 선택 기능으로 넘어가지 않는다.

### 3.5 주 1회는 AI 없이 평가한다

AI를 활용해 학습하되, 매주 최소 한 번은 문서 검색을 제외하고 AI 도움 없이 구현하거나 설명한다. 실제 이해 여부를 확인하기 위한 장치다.

### 3.6 커리큘럼은 Agent보다 우선한다

AI Agent는 당일 설명 방식, 연습 문제, 힌트의 깊이를 조정할 수 있다. 하지만 다음 항목은 임의로 바꾸지 않는다.

- 주차와 티켓의 선후관계
- 필수·심화 구분
- 월별 관문의 완료 조건
- AI 없이 수행해야 하는 평가 구간
- 핵심 학습 항목의 삭제

변경이 필요하다고 판단하면 먼저 `LEARNING_STATE.md`에 변경 제안과 근거를 기록하고 학습자의 동의를 받은 뒤 적용한다.

### 3.7 Agent 역할을 분리한다

| 역할 | 담당 | 제한 |
|---|---|---|
| Tutor Agent | 개념 설명, 질문, 단계별 힌트, 일일 계획 | 시도 전에 완성 코드를 제공하지 않는다. |
| Reviewer Agent | PR diff, 테스트, 보안·장애 위험 검토 | 코드를 대신 고치지 않고 재현 방법과 이유를 먼저 제시한다. |
| Evaluator Agent | 주간·월말 평가, 고정 채점표 적용 | Tutor 대화와 힌트 기록을 입력받지 않는다. |

여러 Agent를 사용할 수 없다면 Tutor 세션을 종료한 뒤 새 대화에서 Evaluator 역할을 수행한다. 이때 커리큘럼, 고정 평가 조건, 제출 시점의 코드와 실행 증거만 제공한다.

---

## 4. 최종 캡스톤 프로젝트

### 4.1 프로젝트 정의

**AI Knowledge Assistant Backend**

사용자가 문서를 등록하고 질문하면 관련 내용을 검색하여 AI 답변을 생성하는 백엔드 서비스다. 대화 이력, 스트리밍 응답, 문서 검색, 외부 AI API 장애 대응을 포함하며 여유가 있을 경우 STT/TTS를 추가한다.

### 4.2 권장 구조

```text
클라이언트
    │ HTTP / SSE
    ▼
Spring Boot 모듈형 모놀리스
    ├─ Conversation: 대화방과 메시지
    ├─ AI Gateway: 외부 LLM/STT/TTS 연동
    ├─ Knowledge: 문서 등록과 검색
    ├─ Job: 비동기 작업과 재처리
    └─ Operations: 로그, 메트릭, 헬스체크
         │                 │
         ▼                 ▼
       MySQL           OpenSearch
    원본 상태          검색용 색인
```

처음부터 마이크로서비스로 나누지 않는다. 하나의 Spring Boot 애플리케이션 안에서 책임을 분리한 모듈형 모놀리스로 구현한다.

### 4.3 필수 기술

- Java
- Spring Boot
- Gradle
- MySQL
- Spring Data JPA와 필요한 범위의 SQL
- Flyway
- JUnit, Mockito, Spring 통합 테스트
- Testcontainers 또는 동등한 실제 의존성 기반 테스트
- 외부 LLM API
- SSE 스트리밍
- OpenSearch
- Docker 및 Docker Compose
- CI 기반 자동 빌드와 테스트
- Spring Actuator와 메트릭
- OpenAPI 문서
- Git 기반 브랜치와 Pull Request 흐름

### 4.4 선택 기술

- STT/TTS
- Kubernetes 기본 배포
- Redis 또는 메시지 브로커
- React/Vue 관리 화면

선택 기술은 **해당 시점까지의 월별 관문과 해당 주차 필수 완료 조건**을 통과한 경우에만 진행한다.

---

## 5. 일일 및 주간 운영 방식

### 5.1 표준 일과

| 시간 | 활동 | 산출물 |
|---|---|---|
| 09:00~11:00 | 개념 학습 | 개인 노트, 질문 목록 |
| 11:00~12:00 | Java 또는 SQL 연습 | 풀이와 오답 기록 |
| 13:00~14:00 | 티켓 분석과 구현 설계 | 구현 계획, 테스트 조건 |
| 14:00~18:00 | 직접 구현 | 커밋 가능한 코드 |
| 19:00~20:00 | 테스트와 디버깅 | 테스트 결과, 오류 기록 |
| 20:00~21:00 | Reviewer Agent 검토, 수정, 상태 갱신 | 리뷰 반영 내역, 다음 날 계획 |

10시간을 모두 고집중 학습으로 채우지 않는다. 식사와 짧은 휴식 외에도 막힌 문제에서 의도적으로 벗어나는 시간을 포함한다.

### 5.2 요일별 리듬

- 월요일: 주간 목표 학습, 티켓 분해, 위험 요소 확인
- 화요일: 핵심 기능 구현
- 수요일: 핵심 기능 구현과 테스트
- 목요일: 예외·장애·성능 조건 보강
- 금요일: AI 없는 제출물 작성, 별도 Evaluator Agent 평가, 데모, 회고
- 토요일 선택: 부족한 개념 3시간 보충
- 일요일: 휴식

### 5.3 막힘 처리 규칙

한 문제에 60분 이상 진전이 없으면 다음 형식으로 정리한 뒤 AI에 질문한다.

```text
하려는 일:
기대 결과:
실제 결과:
확인한 로그와 오류:
이미 시도한 방법:
현재 가설:
도움이 필요한 범위:
```

---

## 6. 12주 커리큘럼

### 시작 전 준비 - 1~2일

첫 주 진도를 시작하기 전에 Tutor Agent와 함께 학습 환경과 운영 파일을 준비한다. 이 기간은 별도 학습 주차로 계산하지 않는다.

#### 준비 항목

- JDK, Docker, Git, IDE 설치와 실행 확인
- Java, Spring Boot, Gradle, MySQL, OpenSearch 버전을 `VERSIONS.md`에 고정
- 선택한 OpenSearch 버전과 Java client의 호환성 및 벡터 검색 지원 여부 확인
- 학습 저장소 생성
- `LEARNING_STATE.md` 생성
- MySQL Docker Compose 실행 확인
- API key와 비밀정보를 제외하는 `.gitignore` 및 환경변수 구성
- 5주 차에 사용할 외부 LLM API 계정, 호출 예산 상한, 사용량 확인 방법 결정

#### 완료 조건

- `java -version`, `git --version`, `docker version`을 실행해 결과를 기록한다.
- Git 저장소를 생성하고 첫 commit을 남긴다.
- MySQL 컨테이너를 시작하고 연결을 확인할 수 있다.
- `VERSIONS.md`와 `LEARNING_STATE.md`가 저장소에 존재한다.
- 원격 저장소와 사용할 CI 서비스를 정해 기록한다.
- 외부 LLM API의 월 예산과 호출 중단 기준을 기록한다.
- 비밀정보가 코드, 설정 기본값, Git 이력에 포함되지 않는다.

## 1개월 차 - Java/Spring/DB 기본기

### 1주 차 - C# 개발자의 Java 전환

#### 목표

- C# 경험을 바탕으로 Java 코드를 읽고 작성한다.
- 테스트를 먼저 실행하고 실패 원인을 확인하는 습관을 만든다.
- 프로젝트 빌드, 테스트, 실행 과정을 이해한다.

#### 필수 학습

- Java 타입, 클래스, 인터페이스, 상속과 조합
- 컬렉션과 제네릭
- 예외 처리
- Stream과 Optional
- 불변 객체와 값 객체
- Java의 동등성 비교
- JVM, 메모리, GC 기초
- Gradle 프로젝트 구조
- JUnit 기본
- C#과 Java의 주요 차이

#### 구현 티켓

- `BE-001`: Gradle 애플리케이션 골격과 wrapper를 구성하고 명령행에서 빌드·테스트한다.
- `BE-001A`: CI에서 Gradle build와 test를 실행하고 실패 시 통과되지 않도록 구성한다.
- `BE-002`: 대화방과 메시지 도메인을 순수 Java로 모델링한다.
- `BE-003`: 메시지 길이, 역할, 생성 시각 규칙을 값 객체와 테스트로 표현한다.

#### 평가

- AI 없이 90분 동안 간단한 메시지 도메인을 구현한다.
- `equals`와 `hashCode`가 필요한 이유를 설명한다.
- checked/unchecked exception의 차이와 사용 판단을 설명한다.
- 전체 테스트를 명령행에서 실행한다.

#### 완료 조건

- 빌드와 테스트가 통과한다.
- 핵심 도메인 규칙에 테스트가 있다.
- 본인이 작성한 모든 코드를 설명할 수 있다.

### 2주 차 - HTTP, REST, Spring Boot

#### 목표

- Spring Boot로 REST API를 구현한다.
- 요청부터 응답까지의 흐름과 의존성 주입을 설명한다.

#### 필수 학습

- HTTP method, status code, header, body
- REST 리소스 설계
- Spring Boot와 자동 설정
- Bean과 의존성 주입
- Controller-Service-Repository 책임
- 요청 DTO와 응답 DTO
- Bean Validation
- 전역 예외 처리
- JSON 직렬화
- OpenAPI 문서 생성 도구와 API 명세 확인
- Controller 테스트

#### 구현 티켓

- `BE-004`: 대화방 생성 및 조회 API를 구현한다.
- `BE-005`: 메시지 등록 및 목록 조회 API를 구현한다.
- `BE-006`: 검증 오류와 도메인 오류의 공통 응답 형식을 구현한다.
- `BE-006A`: 구현한 API를 OpenAPI 문서에서 확인할 수 있도록 구성한다.

#### 평가

- 새로운 요구사항으로 API 하나를 2시간 안에 구현한다.
- 200, 201, 400, 404, 409의 사용 상황을 설명한다.
- 의존성 주입이 테스트와 변경에 주는 장점을 설명한다.

#### 완료 조건

- OpenAPI에서 API를 확인할 수 있다.
- 잘못된 요청이 일관된 형식으로 응답된다.
- Controller와 Service의 책임이 분리되어 있다.

### 3주 차 - MySQL, SQL, JPA, 트랜잭션

#### 목표

- 관계형 데이터 모델을 설계하고 Spring 애플리케이션에 연결한다.
- 트랜잭션과 인덱스가 필요한 이유를 설명한다.

#### 필수 학습

- 테이블, PK, FK, 정규화 기초
- SELECT, JOIN, GROUP BY, Subquery
- 인덱스와 실행 계획
- ACID와 트랜잭션
- 트랜잭션 경계와 기본적인 동시 수정 문제
- JPA Entity와 영속성 컨텍스트
- 기본 연관관계
- Flyway를 이용한 schema migration
- MySQL Testcontainers 기반 통합 테스트

#### 심화 학습

- 격리 수준별 동작 차이
- fetch 전략과 N+1 문제
- 복잡한 연관관계 매핑
- 커서 기반 페이지네이션

#### 구현 티켓

- `BE-007`: Flyway로 schema를 만들고 MySQL 저장소로 교체하며 Testcontainers 통합 테스트를 작성한다.
- `BE-008`: 메시지 목록에 기본 페이지네이션을 적용한다.
- `BE-009`: 대화방 삭제 정책과 트랜잭션을 구현한다.
- `BE-010`: 조회 쿼리에 인덱스를 적용하고 실행 계획을 비교한다.
- `BE-010-ADV` 심화: 메시지 목록에 커서 기반 페이지네이션을 적용한다.

#### 고정 SQL 평가 데이터

Evaluator Agent는 아래 데이터를 `evaluation/sql/seed.sql`로 한 번 생성하고 평가 당일에는 변경하지 않는다.

| 테이블 | 고정 데이터 |
|---|---|
| users | `(1, Kim)`, `(2, Lee)`, `(3, Park)` |
| conversations | `101: user1 ACTIVE 01-01`, `102: user1 ARCHIVED 01-02`, `103: user2 ACTIVE 01-03`, `104: user2 ACTIVE 01-04`, `105: user3 ACTIVE 01-05` |
| messages | `1001~1002: conv101`, `1003: conv102`, `1004~1006: conv103`, `1007: conv104`, `conv105: 없음` |

메시지 역할은 `1001/1003/1004/1006/1007=USER`, `1002/1005=ASSISTANT`로 고정하고 각 대화방 안에서는 ID 순서와 생성 시각 순서가 같게 만든다.

고정 문제:

1. 사용자별 ACTIVE 대화방 수를 구한다. 기대값: `Kim=1, Lee=2, Park=1`.
2. 메시지가 없는 사용자도 포함해 사용자별 메시지 수를 구한다. 기대값: `Kim=3, Lee=4, Park=0`.
3. 각 대화방의 마지막 메시지 ID를 구한다. 기대값: `101=1002, 102=1003, 103=1006, 104=1007, 105=NULL`.
4. 메시지가 없는 대화방을 구한다. 기대값: `105`.
5. ACTIVE 대화방이 2개 이상인 사용자를 구한다. 기대값: `Lee`.
6. 사용자별 가장 최근 ACTIVE 대화방 ID를 구한다. 기대값: `Kim=101, Lee=104, Park=105`.
7. 메시지 역할별 개수를 구한다. 기대값: `USER=5, ASSISTANT=2`.
8. ACTIVE 대화방을 생성일 내림차순으로 2개씩 조회할 때 두 번째 페이지를 구한다. 기대값: `103, 101`.
9. `사용자의 ACTIVE 대화방을 최신순 조회`하는 쿼리에 적절한 복합 인덱스를 제안하고 적용 전후 실행 계획을 비교한다.
10. 대화방과 메시지를 함께 삭제하다 중간 실패한 경우 전체가 rollback되는 통합 테스트를 작성한다.

#### 평가

- AI 없이 고정 SQL 문제 10개를 해결한다: JOIN 2개, GROUP BY/HAVING 2개, Subquery 2개, 인덱스·실행 계획 2개, 트랜잭션 판단 2개.
- 대화방·메시지 스키마를 설명한다.
- 트랜잭션이 중간에 실패하는 통합 테스트를 작성한다.
- JPA가 실행한 SQL을 로그로 확인하고 예상 쿼리와 비교한다.
- 심화: N+1 문제를 로그로 발견하고 수정한다.

#### 완료 조건

- MySQL을 기준으로 모든 통합 테스트가 통과한다.
- 인덱스 적용 전후의 실행 계획 또는 측정 결과가 문서화되어 있다.
- Entity를 API 응답으로 직접 노출하지 않는다.

### 4주 차 - API 품질과 1차 월말 관문

#### 목표

- 첫 번째 버전의 대화 API를 운영 가능한 형태로 정리한다.
- 제한 시간 안에 작은 Spring 기능을 독립적으로 구현한다.

#### 필수 학습

- 단위 테스트와 통합 테스트의 경계
- 테스트 더블
- 인증·인가 기본 개념
- 리소스 소유권 검증
- 요청 크기 제한과 입력 검증
- 로그 레벨과 구조화된 로그
- 환경별 설정과 비밀정보 관리
- Docker와 Docker Compose
- README 작성법

#### 구현 티켓

- `BE-011`: 간단한 인증을 추가하고 다른 사용자의 대화 접근을 거부한다.
- `BE-012`: 애플리케이션과 MySQL을 함께 실행하는 Docker Compose 환경을 완성한다.
- `BE-013`: 애플리케이션 실행과 API 사용 방법을 README에 작성한다.

#### 월말 관문

Tutor 대화를 종료하고 별도 Evaluator Agent가 평가한다. AI 없이 4시간 동안 기존 프로젝트에 다음 `대화방 보관` 기능을 추가한다.

- 대화방을 보관 상태로 변경하는 API
- 기본 목록에서는 보관된 대화방 제외
- 명시적인 조회 조건이 있으면 보관된 대화방 포함
- 다른 사용자의 대화방은 변경 불가
- 같은 보관 요청이 반복되어도 결과가 달라지지 않음
- schema 변경은 Flyway migration으로 관리

통과 조건:

- 요구사항을 API와 DB 변경으로 분해한다.
- 정상·오류 시나리오를 구현한다.
- 단위 테스트 또는 통합 테스트를 작성한다.
- 기존 테스트를 깨뜨리지 않는다.
- 인증되지 않은 요청과 다른 사용자의 리소스 접근을 거부한다.
- 요청 크기와 입력 형식을 제한한다.
- SQL injection이 발생하지 않는 데이터 접근 방식을 사용한다.
- API key와 개인정보가 로그에 남지 않는다.
- 구현 선택을 10분 동안 설명한다.

채점 기준은 기능 40점, 테스트 20점, DB·트랜잭션 15점, 오류·보안 처리 10점, 코드 설명 15점이다. 70점 이상이며 기능·테스트 항목이 각각 절반 이상이어야 통과한다.

통과하지 못하면 5주 차의 외부 API 연동을 시작하기 전에 최대 3일간 부족한 영역을 보충한다.

## 2개월 차 - 외부 AI API 기반 서비스

### 5주 차 - LLM API 연동과 Adapter 설계

#### 목표

- 외부 LLM API를 Spring 서비스에 연결한다.
- 외부 기술이 변경되어도 도메인 코드에 미치는 영향을 제한한다.

#### 필수 학습

- HTTP client와 connection pool 기초
- 외부 API 요청·응답 모델
- Adapter 및 Strategy 패턴
- 설정과 API key 관리
- Mock server 기반 외부 API 테스트
- 토큰 사용량과 비용 기록 개념
- 민감정보 로그 제거
- 실 API 호출 예산과 호출량 제한

#### 구현 티켓

- `BE-014`: LLM Provider 인터페이스와 외부 API Adapter를 구현한다.
- `BE-015`: 사용자 질문을 전송하고 응답을 메시지로 저장한다.
- `BE-016`: 실제 API 없이 연동 흐름을 검증하는 통합 테스트를 작성한다.

#### 완료 조건

- API key가 코드와 Git 이력에 존재하지 않는다.
- 외부 API가 없어도 자동화 테스트가 실행된다.
- Provider 구현을 교체해도 대화 도메인은 수정되지 않는다.
- 개발과 실패 테스트는 Mock을 기본으로 하고, 실 API는 정해진 예산 안에서 검증한다.

### 6주 차 - 스트리밍과 대화 상태

#### 목표

- 긴 AI 응답을 스트리밍한다.
- 클라이언트 연결 종료와 부분 실패를 처리한다.

#### 필수 학습

- SSE의 동작 방식
- 동기·비동기 처리
- 대화 문맥 관리
- 부분 응답 저장 정책
- API 요청 ID와 추적 ID

#### 심화 학습

- 서버 스레드와 연결 자원
- 클라이언트 취소 전파

#### 구현 티켓

- `BE-017`: AI 답변을 SSE로 스트리밍한다.
- `BE-018` 심화: 사용자가 연결을 끊었을 때 요청을 종료하거나 상태를 기록한다.
- `BE-019`: AI 요청과 응답의 처리 상태 및 사용량을 저장한다.

#### 완료 조건

- 스트리밍 정상·실패 테스트가 있다.
- 심화 티켓을 수행한 경우 취소 테스트가 있다.
- 미완료 응답을 DB에서 구분할 수 있다.
- 요청별 처리 시간과 식별자를 로그에서 찾을 수 있다.

### 7주 차 - 회복 탄력성과 관측 가능성

#### 목표

- 외부 AI API가 느리거나 실패해도 서버 전체가 불안정해지지 않도록 한다.
- 로그와 메트릭으로 장애 원인을 찾는다.

#### 필수 학습

- timeout, retry, backoff
- 재시도 가능한 오류와 불가능한 오류
- rate limit
- Spring Actuator
- 로그, 메트릭, 트레이스의 차이
- 기본 부하 테스트

#### 심화 학습

- 멱등성과 중복 요청
- circuit breaker
- connection pool 및 thread pool 튜닝

#### 구현 티켓

- `BE-020`: 외부 API timeout과 제한된 retry를 적용한다.
- `BE-021` 심화: 동일한 요청이 반복되어도 메시지가 중복 생성되지 않도록 한다.
- `BE-022`: 요청 수, 실패 수, 지연 시간 메트릭을 노출한다.
- `BE-023`: 외부 API 지연 및 실패 장애 시나리오를 작성한다.

#### 완료 조건

- 외부 API가 실패해도 DB 상태가 설명 가능한 상태로 남는다.
- 무제한 재시도를 하지 않는다.
- 로그만 보고 하나의 실패 요청 흐름을 추적할 수 있다.
- 부하 테스트의 조건과 결과가 기록되어 있다.

### 8주 차 - STT/TTS 선택 과제와 2차 월말 관문

#### 필수 경로

2개월 차 기능을 안정화한다.

- 테스트 누락 보강
- Docker Compose에 필요한 의존성 통합
- API 오류 문서화
- 외부 API 장애 대응 점검
- 보안 점검

#### 선택 경로

필수 경로가 완료된 경우 다음 파이프라인을 구현한다.

```text
음성 파일 업로드 → STT → LLM → TTS → 음성 결과 조회
```

- `BE-024`: 음성 파일 업로드와 상태 조회를 구현한다.
- `BE-025`: STT와 TTS를 각각 독립 Adapter로 구현한다.
- `BE-026`: 단계별 실패 상태와 재처리 정책을 구현한다.

선택 경로를 수행할 때도 허용할 파일 형식, 최대 크기, 저장 기간을 먼저 정하고 검증한다.

#### 월말 관문

Tutor 대화를 종료하고 별도 Evaluator Agent가 평가한다. AI 없이 4시간 동안 외부 AI API가 다음과 같이 동작하도록 Mock을 구성하고 시스템을 검증한다.

평가 설정은 요청 timeout 1초, 최대 호출 횟수 `최초 1회 + 재시도 2회`로 고정한다. 테스트 환경의 backoff는 10ms와 20ms를 사용한다. 일반적인 4xx와 잘못된 응답 형식은 재시도하지 않는다. 429 응답에 `Retry-After`가 있으면 적용하되 테스트에서는 가상 시간 또는 짧은 값으로 검증한다.

| Mock 조건 | 기대 처리 |
|---|---|
| 정상 응답과 500ms 지연 | 1초 안에 성공하고 처리 시간을 기록 |
| 1초 timeout 초과 | 요청을 종료하고 실패 상태 및 요청 ID 기록 |
| 429 rate limit | 최대 3회 호출 후 `AI_RATE_LIMITED` 오류와 503 응답으로 변환 |
| 일시적인 5xx | 최대 3회 호출 후 `AI_PROVIDER_UNAVAILABLE` 오류와 502 응답으로 변환 |
| 잘못된 응답 형식 | 재시도하지 않고 외부 응답 오류로 기록 |
| 스트리밍 중 연결 종료 | 미완료 응답임을 구분하고 내부 오류를 노출하지 않음 |

통과 조건:

- 오류 유형에 따라 적절히 재시도하거나 즉시 실패한다.
- 사용자에게 내부 구현을 노출하지 않는 오류를 반환한다.
- DB에 설명 불가능한 처리 상태가 남지 않는다.
- 테스트와 로그로 처리 결과를 증명한다.

채점 기준은 정상 기능 25점, 장애별 기대 처리 35점, 자동화 테스트 20점, 로그·상태 기록 10점, 코드 설명 10점이다. 70점 이상이며 timeout, 429, 5xx 테스트가 모두 통과해야 한다.

## 3개월 차 - OpenSearch, 검색 기반 AI, 운영

### 9주 차 - OpenSearch 기초

#### 목표

- 관계형 DB와 검색엔진의 역할 차이를 이해한다.
- 문서를 색인하고 기본 검색 API를 구현한다.

#### 필수 학습

- index, document, field, mapping
- inverted index
- analyzer와 tokenization
- full-text query와 filter
- 정렬과 페이지네이션
- bulk indexing
- 검색 결과 점수
- MySQL을 원본으로 두는 이유
- 색인 상태 `PENDING/SUCCEEDED/FAILED`

#### 구현 티켓

- `BE-027`: 일반 텍스트 또는 Markdown 지식 문서의 등록과 조회 API를 구현한다.
- `BE-028`: 색인 상태와 실패 사유를 기록하며 문서를 OpenSearch에 색인한다.
- `BE-029`: 키워드와 필터를 지원하는 검색 API를 구현한다.
- `BE-030`: 검색 결과와 점수를 API로 반환한다.

#### 완료 조건

- mapping을 명시적으로 관리한다.
- 검색과 필터의 차이를 설명할 수 있다.
- OpenSearch 장애가 원본 데이터 손실로 이어지지 않는다.
- PDF 파싱과 OCR은 필수 범위에 포함하지 않는다.

### 10주 차 - RAG와 검색 품질

#### 목표

- 검색 결과를 이용해 근거 기반 AI 응답을 생성한다.
- 검색 품질을 느낌이 아닌 테스트 데이터로 비교한다.

#### 필수 학습

- 문서 분할 전략
- 임베딩과 벡터 검색 개념
- 키워드 검색과 하나의 벡터 검색 방식
- top-k와 score
- 검색 결과를 프롬프트에 포함하는 방법
- 출처 표시
- 간단한 검색 평가 데이터셋
- hallucination과 근거 부족 처리
- 검색 문서를 신뢰할 수 없는 입력으로 취급하는 prompt injection 대응 원칙

#### 심화 학습

- 하이브리드 검색과 결과 튜닝
- PDF 파싱 또는 OCR 입력

#### 구현 티켓

- `BE-031`: 문서를 조각으로 분리하고 색인한다.
- `BE-032`: 질문과 관련된 문서 조각을 검색한다.
- `BE-033`: 검색 결과를 사용해 답변을 생성하고 근거 문서를 반환한다.
- `BE-034`: 고정된 대표 질문 세트로 검색 품질을 비교한다.
- `BE-034-ADV` 심화: 키워드와 벡터 결과를 결합하고 검색 설정을 튜닝한다.

#### 완료 조건

- 답변과 근거 문서를 함께 반환한다.
- 관련 문서가 없을 때 답변 정책이 정의되어 있다.
- 검색 설정 변경 전후 결과를 같은 질문 세트로 비교한다.
- 검색 문서 안의 명령을 시스템 지시로 실행하지 않는다.

### 11주 차 - 데이터 일관성, 재처리, 성능

#### 목표

- MySQL과 OpenSearch의 불일치를 탐지하고 복구한다.
- 성능 문제를 측정한 뒤 개선한다.

#### 필수 학습

- eventual consistency
- 실패 재시도와 dead-letter 개념
- 부하 테스트와 병목 분석
- connection pool과 thread pool 기초 점검

#### 심화 학습

- reindex와 index alias
- 운영 중 mapping 변경 전략
- Kubernetes의 Deployment, Service, ConfigMap, Secret, Probe 기초

#### 구현 티켓

- `BE-036`: 실패한 색인을 조회·재처리하고, MySQL 전체 원본으로 OpenSearch index를 재구축하는 관리 작업을 구현한다.
- `BE-037` 심화: 전체 문서를 새 index에 재색인하고 alias를 전환한다.
- `BE-038`: 대화 또는 검색 API의 부하 테스트를 수행하고 병목 하나를 개선한다.
- `BE-039` 선택: 로컬 Kubernetes에 애플리케이션을 배포한다.

#### 완료 조건

- OpenSearch의 데이터를 모두 삭제해도 MySQL에서 복구할 수 있다.
- 같은 작업을 재실행해도 데이터가 무한히 중복되지 않는다.
- 성능 개선 전후의 조건과 수치가 문서화되어 있다.

#### 3차 월말 관문

Tutor 대화를 종료하고 별도 Evaluator Agent가 다음 고정 과제를 진행한다.

1. OpenSearch index를 삭제한 뒤 MySQL 원본에서 복구한다.
2. 색인 요청 하나를 의도적으로 실패시키고 상태를 확인한 뒤 재처리한다.
3. AI 없이 4시간 동안 문서 `category` 필터 요구사항을 API, mapping, 테스트에 추가한다.
4. 아래 고정 질문 세트로 검색 결과를 검증한다.

| 문서 ID | 문서 핵심 내용 | 대표 질문 | 기대 결과 |
|---|---|---|---|
| DOC-01 | 계정 삭제는 설정에서 신청하며 7일 후 완료 | 계정을 삭제하려면 어떻게 하나요? | DOC-01이 상위 결과 |
| DOC-02 | 파일 업로드 한도는 파일당 20MB | 업로드 가능한 최대 파일 크기는? | DOC-02가 상위 결과 |
| DOC-03 | 고객 지원은 평일 09~18시에 운영하며 주말과 공휴일에는 운영하지 않음 | 주말에도 고객 지원을 하나요? | DOC-03이 상위 결과 |
| DOC-04 | 대화 기록은 30일 동안 보관 | 대화 내용은 언제 삭제되나요? | DOC-04가 상위 결과 |
| DOC-05 | API 오류 발생 시 요청 ID를 고객센터에 전달 | 오류 문의에는 어떤 정보가 필요한가요? | DOC-05가 상위 결과 |
| 해당 없음 | 위 문서에 요금제 가격 정보 없음 | 프리미엄 요금제 가격은 얼마인가요? | 근거 부족으로 답변 보류 |

통과 조건:

- 복구와 재처리 과정이 자동화 테스트 또는 재현 가능한 명령으로 검증된다.
- 필터가 없는 기존 검색 동작을 깨뜨리지 않는다.
- 문서가 있는 대표 질문 5개 모두에서 기대 문서가 상위 3개 결과에 포함된다.
- 요금제 가격 질문에는 근거가 부족하다는 정책을 적용한다.
- 테스트, 로그, 검색 결과를 평가 증거로 저장한다.

채점 기준은 전체 복구·실패 재처리 30점, `category` 기능 25점, 자동화 테스트 20점, 검색 fixture 결과 15점, 로그·코드 설명 10점이다. 70점 이상이며 전체 복구, 기존 검색 회귀 테스트, 문서가 있는 질문 5개의 상위 3개 검색, 근거 없는 질문의 답변 보류가 모두 통과해야 한다.

### 12주 차 - 안정화, 종합 평가, 인수인계

#### 목표

- 새로운 기술 추가를 중단하고 프로젝트를 다른 개발자가 실행·검토할 수 있게 만든다.
- 현업에서 작은 업무를 받을 준비가 되었는지 평가한다.

#### 필수 작업

- 전체 코드 리팩터링
- 불필요한 기능과 의존성 제거
- 전체 테스트 안정화
- 로컬 실행 절차 검증
- API 명세 정리
- 아키텍처 다이어그램 작성
- 주요 설계 결정 기록
- 알려진 한계와 후속 과제 작성
- 장애 대응 Runbook 작성
- 15분 프로젝트 발표 준비

#### 종합 실습

처음 보는 요구사항 하나를 받아 다음 과정을 수행한다.

1. 요구사항 질문 작성
2. 영향 범위와 구현 계획 작성
3. 기능 구현
4. 테스트 작성
5. PR 설명 작성
6. AI를 사용하지 않은 제출 시점의 commit을 생성한다.
7. 별도 Reviewer Agent의 리뷰를 받는다.
8. 리뷰를 이해하고 수정한다.
9. 데모한다.
10. 구현 선택과 한계를 설명한다.

#### 최종 제출물

- 실행 가능한 Git 저장소
- README
- Docker Compose 실행 환경
- OpenAPI 문서
- 자동화 테스트
- DB schema 또는 ERD
- 아키텍처 다이어그램
- 의미 있는 ADR 1~3개
- 부하 테스트 보고서
- 장애 시나리오 및 대응 기록
- 12주 학습 회고

---

## 7. 평가 기준

### 7.1 평가 수준

| 수준 | 정의 |
|---:|---|
| 0 | 개념을 모르고 수행할 수 없다. |
| 1 | 상세한 안내나 완성 예제가 있어야 수행한다. |
| 2 | 공식 문서와 제한된 도움을 이용해 수행한다. |
| 3 | 요구사항을 스스로 분해하고 독립적으로 수행한다. |
| 4 | 트레이드오프를 설명하고 다른 사람의 구현도 개선할 수 있다. |

교육 종료 시 모든 항목에서 3~4를 요구하지 않는다. 핵심 영역에서 최소 2, 일부 작은 업무에서 3에 도달하면 현업 OJT가 가능한 상태로 본다.

### 7.2 평가 항목과 비중

| 평가 영역 | 비중 | 최종 기대 수준 |
|---|---:|---:|
| Java와 Spring | 20% | 2 이상 |
| DB, SQL, 트랜잭션 | 15% | 2 이상 |
| HTTP와 API 설계 | 10% | 2 이상 |
| 테스트와 코드 품질 | 15% | 2 이상 |
| 문제 해결과 디버깅 | 15% | 2 이상 |
| 외부 AI API와 OpenSearch | 10% | 2 이상 |
| 운영 안전성과 관측 가능성 | 10% | 1 이상 |
| 커뮤니케이션과 문서화 | 5% | 2 이상 |

수준을 점수로 환산할 때는 `0=0점`, `1=40점`, `2=70점`, `3=90점`, `4=100점`을 사용한다. 각 영역의 환산 점수에 비중을 곱한 값을 더해 전체 점수를 계산한다.

Evaluator Agent는 인상이나 답변의 유창함보다 다음 증거를 우선한다.

- Java/Spring: AI 없이 구현한 commit, 빌드 결과, 코드 설명
- DB: migration, 실행 SQL, 실행 계획, 트랜잭션 테스트
- HTTP/API: OpenAPI 명세, 정상·오류 응답, 호환성
- 테스트: 자동화 테스트 실행 결과와 실패 재현 능력
- 문제 해결: 가설, 확인 순서, 로그와 수정 전후 증거
- AI/OpenSearch: Mock 테스트, 검색 fixture 결과, 색인 복구
- 운영: CI 결과, 로그, 메트릭, Docker 재현
- 커뮤니케이션: PR 설명, ADR, 일일·주간 상태 기록

### 7.3 학습 완료 자기점검

#### 현업 OJT 준비 수준

- 전체 환산 점수 70점 이상
- Java/Spring, DB, 테스트, 문제 해결이 모두 수준 2 이상
- 3회의 월별 관문을 모두 통과하거나 미달 항목을 재평가하여 해소함
- AI 없이 작은 기능을 구현하고 설명할 수 있음
- 치명적인 보안 문제나 데이터 손실 가능성을 인지하고 수정함

#### 보완 후 현업 OJT 가능

- 전체 환산 점수 60~69점
- 핵심 영역 중 한 항목만 수준 1이고 명확한 보완 계획이 있음
- 현업 첫 달에 배정 가능한 업무 범위를 구체적으로 제한할 수 있음

#### 핵심 영역 추가 학습 필요

- 핵심 영역 중 둘 이상이 수준 1 이하
- AI가 작성한 코드를 설명하지 못함
- 자동화 테스트 없이 정상 동작만 확인함
- 막힌 상황과 위험을 장시간 공유하지 않음
- 데이터 손실, 비밀정보 노출, 무제한 재시도와 같은 중대 위험을 반복함

점수는 판단을 돕는 도구이며, 소수점 차이만으로 현업 투입 여부를 결정하지 않는다. 코드, 테스트, 설명, 작업 기록을 함께 검토한다.

---

## 8. AI 도구 사용 가이드

### 8.1 허용하는 사용

- 개념을 다른 방식으로 설명받기
- 공식 문서 탐색 경로 찾기
- 연습 문제와 장애 시나리오 생성
- 작성한 설계와 코드 리뷰
- 테스트 누락 확인
- 오류 원인에 대한 가설 확장
- 구술 면접과 회고 진행

### 8.2 제한하는 사용

- 시도 전에 완성 코드를 통째로 생성
- 이해하지 못한 코드를 저장소에 반영
- AI 답변을 공식 문서 확인 없이 사실로 단정
- 테스트가 통과한다는 AI의 설명을 실제 실행 대신 사용
- API key, 개인정보, 회사 코드나 비공개 로그 입력
- AI가 작성한 설계 결정을 이유 없이 수용

### 8.3 시작 프롬프트

학습 저장소를 연 AI Agent의 첫 세션에 다음 프롬프트를 전달한다.

```text
이 저장소의 `3개월_자기주도_BE_전환_프로그램.md` 전체를 먼저 읽어라.
이 문서를 요약하는 데서 멈추지 말고 12주 커리큘럼의 운영 Agent로 행동하라.

첫 작업:
1. 커리큘럼의 필수·심화 항목과 월별 관문을 확인한다.
2. `LEARNING_STATE.md`와 `VERSIONS.md`가 없으면 문서의 템플릿으로 생성한다.
3. 시작 전 준비 항목을 순서대로 점검한다.
4. 현재 상태에서 수행할 티켓 하나만 제안한다.
5. 티켓을 시작하기 전에 학습자의 접근 방법과 테스트 계획을 질문한다.

운영 규칙:
- 커리큘럼의 순서와 통과 조건을 임의로 낮추거나 삭제하지 않는다.
- 학습자가 시도하기 전에 완성 코드를 제공하지 않는다.
- 설명과 예시는 현재 티켓에 필요한 범위로 제한한다.
- 빌드, 테스트, 로그, 실행 결과를 완료의 증거로 요구한다.
- 매 세션 시작 시 상태 파일을 읽고 종료 시 갱신한다.
- 평가는 별도 Evaluator Agent 또는 새 대화에서 수행하도록 안내한다.
```

### 8.4 `LEARNING_STATE.md` 템플릿

```markdown
# Learning State

- 마지막 갱신:
- 현재 주차:
- 진행 중 티켓:
- 현재 월별 관문 상태: 미진행 / 통과 / 보충 필요

## 완료한 항목과 증거

- 항목:
  - commit 또는 파일:
  - 실행 명령:
  - 결과:

## 실패한 테스트와 미해결 결함

- 증상:
- 현재 가설:
- 다음 확인:

## AI 도움 기록

- 도움을 받은 부분:
- AI 없이 다시 설명하거나 구현 가능한지:

## 평가 결과와 보충 과제

- 평가 Agent:
- 점수와 근거:
- 보충할 항목:

## 다음 행동

1.
```

### 8.5 `VERSIONS.md` 템플릿

```markdown
# Versions

| 기술 | 고정 버전 | 공식 지원 문서 | 선택 이유 | 확인일 |
|---|---|---|---|---|
| Java | | | LTS | |
| Spring Boot | | | Java 호환성 | |
| Gradle | | | Spring Boot 호환성 | |
| MySQL | | | 교육용 기준 RDBMS | |
| OpenSearch | | | client 호환성 | |

버전을 변경할 때는 변경 이유, migration 영향, 테스트 결과를 기록한다.
```

### 8.6 공통 AI 튜터 프롬프트

```text
당신은 Java/Spring 백엔드 전환 교육의 튜터다.

학습자:
- Unity/C# 상용 개발 경력 약 10년
- Java와 SQL은 학교 수업 수준
- 목표는 AI 서비스 개발팀의 실제 업무를 받을 수 있는 수준

현재 주차와 주제:
[주차와 주제를 입력]

오늘의 티켓:
[티켓 내용을 입력]

규칙:
1. 바로 완성 코드를 제공하지 않는다.
2. 먼저 학습자의 접근 방법과 현재 이해를 질문한다.
3. 필요한 개념을 C# 경험과 비교해 설명한다.
4. 힌트는 작은 단계로 제공한다.
5. 사실 확인이 필요한 내용은 공식 문서 경로를 제시한다.
6. 정상 흐름뿐 아니라 실패, 동시성, 테스트 조건을 질문한다.
7. 마지막에는 학습자가 직접 설명해야 할 확인 질문 5개를 제시한다.
```

### 8.7 코드 리뷰 프롬프트

```text
다음 변경사항을 현업 Spring 백엔드 Pull Request처럼 리뷰하라.

검토 순서:
1. 요구사항 충족 여부
2. 버그와 데이터 손실 위험
3. 트랜잭션과 동시성
4. 외부 API 실패 처리
5. 테스트 누락
6. 보안과 민감정보
7. 가독성과 책임 분리
8. 불필요한 복잡성

각 의견에 심각도(Blocker/Major/Minor/Question)를 표시한다.
해결 코드를 바로 작성하지 말고 문제의 이유와 재현 방법을 먼저 설명한다.
잘한 점을 별도 항목으로 나열하지 말고, 위험과 개선점에 집중한다.

티켓:
[요구사항]

설계 의도:
[학습자의 설명]

변경 코드 또는 diff:
[코드]
```

### 8.8 주간 구술 평가 프롬프트

```text
이번 주 학습 범위에 대해 30분 분량의 백엔드 구술 평가를 진행하라.

범위:
[주간 학습 범위]

규칙:
- 한 번에 질문 하나만 한다.
- 단순 정의보다 실제 상황 판단을 묻는다.
- 답변이 모호하면 후속 질문으로 확인한다.
- 정답을 바로 말하지 않는다.
- 마지막에 0~4 수준으로 평가하고 근거를 제시한다.
- 다음 주 전에 보완할 항목을 최대 3개로 제한한다.
```

### 8.9 장애 훈련 프롬프트

```text
내 Spring Boot 서비스에 발생할 수 있는 장애 시나리오 하나를 제시하라.
현재 구성은 MySQL, 외부 LLM API, OpenSearch, Docker Compose다.

로그와 증상을 단계적으로만 제공하고 원인은 알려주지 않는다.
내가 가설, 확인 명령, 추가로 필요한 정보를 제시하면 그 결과를 돌려준다.
장애 분석이 끝나면 다음을 평가한다.
- 가설의 질
- 확인 순서
- 로그와 지표 활용
- 사용자 영향 최소화
- 재발 방지책
```

### 8.10 독립 Evaluator Agent 프롬프트

```text
당신은 이 교육의 독립 평가자다. Tutor Agent의 대화나 힌트 기록은 제공받지 않는다.

입력으로 받을 것:
- 커리큘럼 원문
- 현재 주차 또는 월말 관문의 고정 요구사항
- 평가 시작 시점의 commit
- 학습자가 작성한 PR 설명
- 빌드·테스트·API·SQL·검색 실행 증거

평가 규칙:
1. 커리큘럼의 완료 조건과 고정 채점표만 사용한다.
2. 실행되지 않은 코드와 학습자의 주장만으로 통과시키지 않는다.
3. 먼저 AI 없이 수행한 제출물을 평가한다.
4. 0~4 수준과 환산 점수를 계산하고 근거 파일·테스트를 연결한다.
5. 치명적인 보안·데이터 손실 위험은 총점과 관계없이 Blocker로 표시한다.
6. 통과시키기 위해 기준을 낮추지 않는다.
7. 미달 시 보충 과제를 최대 3개로 제한한다.
8. 평가가 끝난 후에만 정답 방향과 개선 힌트를 제공한다.
```

---

## 9. Git과 작업 기록 규칙

- 티켓 하나당 브랜치 하나를 사용한다.
- 커밋은 실행 가능한 작은 단위로 나눈다.
- 생성 파일, 비밀정보, IDE 개인 설정을 커밋하지 않는다.
- 각 티켓은 Pull Request 설명을 작성한다.
- AI 리뷰 의견과 반영 여부를 기록한다.
- 큰 설계 결정은 ADR로 남긴다.

Pull Request 템플릿:

```text
## 변경 목적

## 구현 내용

## 주요 설계 결정과 이유

## 테스트 방법과 결과

## 실패 및 예외 처리

## 영향 범위

## 알려진 한계

## 리뷰어에게 확인받고 싶은 부분
```

---

## 10. 학습 기록과 주간 회고

매일 다음 항목을 10분 안에 기록한다.

```text
오늘 완료한 것:
가장 어려웠던 문제:
처음 세운 가설:
실제 원인:
AI의 도움을 받은 부분:
AI 답변을 검증한 방법:
내일 가장 먼저 할 일:
```

매주 금요일에는 다음을 기록한다.

- 완료한 티켓과 미완료 티켓
- 직접 설명할 수 있는 개념
- AI 없이는 수행하기 어려운 부분
- 발생한 버그와 원인
- 다음 주에 반복하지 않을 실수
- 다음 주 진입 가능 여부

미완료 티켓을 숨기기보다 원인과 범위를 명확히 기록하는 것을 더 중요하게 평가한다.

---

## 11. 일정 조정 규칙

### 진도가 늦을 때 줄이는 순서

1. React/Vue 화면
2. Kubernetes 실습
3. STT/TTS
4. 벡터 검색 고도화
5. 고급 디자인 패턴

다음 항목은 줄이지 않는다.

- Java 기본기
- Spring REST API
- SQL과 트랜잭션
- 자동화 테스트
- 외부 API 오류 처리
- OpenSearch 기본 색인과 검색
- Docker 실행 환경
- 로그와 디버깅

### 진도가 빠를 때 확장하는 순서

1. 테스트와 장애 시나리오 강화
2. SQL 및 검색 성능 분석
3. STT/TTS
4. Kubernetes 배포
5. 간단한 관리 화면
6. 메시지 브로커 또는 Redis

빠른 진도를 새로운 기술의 수로 판단하지 않는다. 같은 기능을 더 안전하고 설명 가능하게 만드는 것을 우선한다.

---

## 12. 교육 종료 후 현업 OJT 권장 범위

이 내용은 대상 팀의 실제 계획이 아니라 교육 후 본인이 기대할 수 있는 적응 순서다.

### OJT 1개월 차

- 프로젝트 실행과 구조 파악
- 테스트 보강
- 작은 버그 수정
- 조회 API
- 로그와 메트릭 추가
- 문서 보완

### OJT 2개월 차

- 작은 기능 하나를 요구사항부터 배포까지 수행
- 외부 AI API 연동 수정
- 검색 조건 또는 운영 기능 추가
- 리뷰 피드백 반복 반영

### OJT 3개월 차

- 작은 기능 영역의 오너십
- 성능 또는 운영 문제 하나 개선
- 배포 후 모니터링
- 장애 재발 방지 또는 자동화
- 팀 내부 문서와 공유 자료 작성

현업에서 좋은 평가를 받는 핵심은 처음부터 큰 기능을 혼자 개발하는 것이 아니다. 작업 상태와 위험을 일찍 공유하고, 작은 변경을 테스트와 함께 안전하게 전달하는 것이다.
